/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['github.com', 'images.unsplash.com'], // Add other domains if needed
    },
};

export default nextConfig;
